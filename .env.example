# Supabase 配置
# 从 Supabase 项目设置中获取这些值
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# 注意：这些是前端使用的环境变量，会暴露给客户端
# 请确保使用的是 Supabase 的 anon key，而不是 service_role key
# service_role key 应该只在服务端使用（如 GitHub Actions 中的 Python 脚本）